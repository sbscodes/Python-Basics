a=dir(__builtins__)

for aa in a:
    print(aa)

"""
Python has following builtins functinos and keywords
False
await
else
import
pass		
None
break
except
in
raise
True
class
finally
is
return
and
continue
for
lambda
try
as
def
from
nonlocal
while
assert
del
global
not
with
async
elif
if
or
yield
"""
"""
1)Identifiers can be a combination of letters
 in lowercase (a to z) or uppercase (A to Z) or digits (0 to 9) or 
 an underscore _.
 
2)An identifier cannot start with a digit. 1variable is invalid,
but variable1 is perfectly fine.
3)We cannot use special symbols like !, @, #, $, % etc
"""
Value=5
#here Value is identifier